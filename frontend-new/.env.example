# SONAR-INTEL Frontend Environment Variables
# Copy this file to .env.local and set values for your deployment.

# Backend API base URL (leave empty for same-origin proxy via Vite)
# For local development with Vite proxy, leave empty or set to http://localhost:8000
VITE_API_URL=
